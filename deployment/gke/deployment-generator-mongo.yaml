apiVersion: v1
kind: Service
metadata:
  name: gene-mongo
spec:
  ports:
  - port: 27017
    name: gene-mongo
  selector:
    app: gene-mongo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gene-mongo-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard-exp  
---
apiVersion: apps/v1beta2 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: gene-mongo
spec:
  selector:
    matchLabels:
      app: gene-mongo
  strategy:
    type: Recreate      
  template:
    metadata:
      labels:
        app: gene-mongo
    spec:
      containers:
      - image: mongo:4
        name: gene-mongo        
        ports:
        - containerPort: 27017
          name: gene-mongo
        volumeMounts:
        - name: gene-mongo-persistent-storage
          mountPath: /data/db
        resources:
          requests:
            cpu: "50m"
      volumes:
      - name: gene-mongo-persistent-storage
        persistentVolumeClaim:
          claimName: gene-mongo-pv-claim      